name: Test Workflow

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Simple test
        run: echo "GitHub Actions is working!"
